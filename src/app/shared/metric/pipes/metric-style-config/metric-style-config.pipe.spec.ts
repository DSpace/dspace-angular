import { MetricStyleConfigPipe } from './metric-style-config.pipe';

describe('MetricStyleConfigPipe', () => {
  it('create an instance', () => {
    const pipe = new MetricStyleConfigPipe();
    expect(pipe).toBeTruthy();
  });
});
