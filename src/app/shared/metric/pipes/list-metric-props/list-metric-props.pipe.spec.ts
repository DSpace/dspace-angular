import { ListMetricPropsPipe } from './list-metric-props.pipe';

describe('ListMetricPropsPipe', () => {
  it('create an instance', () => {
    const pipe = new ListMetricPropsPipe();
    expect(pipe).toBeTruthy();
  });
});
