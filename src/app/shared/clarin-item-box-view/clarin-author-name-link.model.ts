/**
 * Redirect the user after clicking on the `Author`.
 * This class is separated into single file because of circular dependencies.
 */
export class AuthorNameLink {
  name: string;
  url: string;
  isAuthority: boolean;
}
