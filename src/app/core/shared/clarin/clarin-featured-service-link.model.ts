/**
 * The class represents FeaturedServiceLink in the ref box (Item View)
 */
export class ClarinFeaturedServiceLink {
  /**
   * The language e.g. `Arabic`
   */
  key: string;

  /**
   * URL link for redirecting to the featured service page
   */
  value: string;
}
