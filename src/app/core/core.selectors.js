import { createFeatureSelector } from '@ngrx/store';
/**
 * Base selector to select the core state from the store
 */
export var coreSelector = createFeatureSelector('core');
//# sourceMappingURL=core.selectors.js.map